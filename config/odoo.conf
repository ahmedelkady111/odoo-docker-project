[options]
; This section contains general options for the Odoo server
; admin_passwd = admin # Removed: Set master password manually or via ODOO_ADMIN_PASSWORD env var if supported/needed

addons_path = /mnt/extra-addons,/usr/lib/python3/dist-packages/odoo/addons

; logfile = /var/log/odoo/odoo.log # Removed: Logging will be configured for stdout/stderr later
log_handler = :INFO # Log to stdout/stderr
; Default log level
log_level = info

xmlrpc_port = 8069

; Database connection settings are now handled via environment variables (HOST, PORT, USER, PASSWORD)
; db_host = 
; db_port = 
; db_user = 
; db_password =

